services:
  home-assistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: home-assistant
    restart: unless-stopped
    privileged: false
    environment:
      TZ: ${TZ:-Etc/UTC}
    networks:
      - whatsfordinner_net
    volumes:
      - "/volume1/docker/home-assistant:/config"
      - /etc/localtime:/etc/localtime:ro
    mem_limit: 2g
    cpus: 2.0
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

networks:
  whatsfordinner_net:
    external: true   # created by the whatsfordinner stack, not this one